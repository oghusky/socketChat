<nav class="navbar navbar-expand-lg bg-light navbar-light fixed-top container">
  <a class="navbar-brand" href="/user/all_users">Chatbar</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler"><i class="fas fa-bars"></i></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <% if(!user){%>
      <li class="nav-item">
        <a class="nav-link" href="/login">Login</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/register">Register</a>
      </li>
      <%}else{%>
      <li class="nav-item">
        <a class="nav-link" id="user-name" href="/user/<%= user.id %>"><%= user.username.toUpperCase() %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/chat">Chat</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/logout">Logout</a>
      </li>
      <%}%>
    </ul>
  </div>
</nav>
<div class="container">
  <!-- error &success show up as empty arrays -->
  <!-- setting it >0 ensures theres something in there -->
  <% if(success && success.length > 0){ %>
      <div class="alert alert-success" role="alert">
        <%= success %>
        <% console.log("Success:" + success.length) %>
      </div>
      <% } %>
      <% if(error && error.length > 0){ %>
      <div class="alert alert-danger" role="alert">
        <%= error %>
        <% console.log("Error: " + error.length) %>
      </div>
      <% } %>
  </div>
  <%if(path === "/chat"){%>
  <h4 id="room-name"><%= roomName %> <span id="room-count"></span></h3>
    <%}%>