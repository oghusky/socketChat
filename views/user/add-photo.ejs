<% if(currentUser.id === user.id){%>
<h3 class="text-center">Add Photo</h3>
<form class="container add-photo" action="/user/add_photo=<%= currentUser.id %>" method="POST"
  enctype="multipart/form-data" autocomplete="off">
  <input type="file" name="userimg" id="userimgname">
  <p class="mt-3"><a class="mr-3" href="/user/<%= currentUser.id %>">Cancel</a>
    <button class="btn btn-primary ml-3" id="photo-submit">SUBMIT</button></p>
</form>
<%}else{%>
<%-include("../error/error500.ejs")%>
<%}%>